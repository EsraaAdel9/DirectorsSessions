@model WebApp.Models.SessionReport

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h4>هل تريد حذف هذا المحضر بالفعل ؟</h4>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NewSession.Session_Num)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NewSession.Session_Num)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SessionFile)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SessionFile)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color" style="text-align:center">
            <input type="submit" value="حذف المحضر" class="btn btn-danger" />
            <br/>
            <a href="@Url.Action("Index")" class="btn btn-previous pull-right"><i class="fa fa-backward pull-right" style="padding-top: 1px; padding-right: 5px;"></i>العودة لقائمة المحاضر</a>
        </div>
    }
</div>
